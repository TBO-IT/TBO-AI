{
  "name": "TBO-project",
  "version": "1.0.0",
  "packageManager": "pnpm@8.6.12",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:doppler": "doppler run -- pnpm dev",
    "build:web": "cd apps/web && npm install && npm run build",
    "build:api": "cd apps/api && npm install && npm run build",
    "render-build": "pnpm install --prod=false && pnpm run build",
    "postinstall": "pnpm rebuild duckdb --filter api || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "turbo": "^2.10.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=6.0.0"
  }
}